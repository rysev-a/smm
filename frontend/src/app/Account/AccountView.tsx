const AccountView = ({ email, password }) => (
  <div className="account">
    <h2>Account View</h2>
    <div> email: {email}</div>
    <div> password: {password} </div>
  </div>
);

export default AccountView;
